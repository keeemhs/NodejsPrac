<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <Style>
        span {
            color: blueviolet;
            font-weight: 700;
        }
    </Style>
</head>
<body>
    <div><span><%= userInfo.name %></span>님 환영합니다.</div>
    <div>성별은 <span><%= userInfo.gender %></span>이군요!!</div>
    <br />
    <div><span><%= userInfo.year %></span>년
    <span><%= userInfo.month %></span>월
    <span><%= userInfo.day %></span>일</div>
    <br />
    <div>관심사는 <span><%= userInfo.hobby %></span>이군요!!</div>
    <br />
    <div>좋아하는 색상은 <span><%= userInfo.color %></span></div>
    <div>좋아하는 숫자는 <span><%= userInfo.number %></span></div>
    <a href="/">홈으로 이동</a>
</body>
</html>